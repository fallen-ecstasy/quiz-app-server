import { Router } from "express";

const quizRouter = Router();

/* Quiz Routes */

// ? - `GET /api/quizzes`
// ? - `GET /api/quizzes/:id`
// ? - `POST /api/quizzes`
// ? - `PUT /api/quizzes/:id`
// ? - `DELETE /api/quizzes/:id`
// ? - `POST /api/quizzes/:quizId/share/facebook`
// ? - `POST /api/quizzes/:quizId/share/twitter`
// ? - `POST /api/quizzes/:quizId/share/linkedin`
// ? - `GET /api/quizzes/:quizId/shareable-link`
// ? - `POST /api/quizzes/:quizId/reviews`
// ? - `GET /api/quizzes/:quizId/reviews`
// ? - `GET /api/quizzes/:quizId/analytics`

export default quizRouter;